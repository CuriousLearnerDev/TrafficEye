# TrafficEye GUI — Python runtime dependencies
# Python >= 3.10 recommended

# GUI
PyQt6>=6.6.0
PyQt6-Charts>=6.6.0

# HTTP / Agent
requests>=2.31.0
httpx>=0.27.0
websockets>=12.0

# Config / templates / reports
PyYAML>=6.0.1
Jinja2>=3.1.0
pdfkit>=1.0.0
python-docx>=1.1.0

# GeoIP / domain / logging / models
geoip2>=4.8.0
maxminddb>=2.6.0
tldextract>=5.1.0
python-dateutil>=2.9.0
loguru>=0.7.0
pydantic>=2.6.0

# Optional: Windows EXE packaging (not required for source run)
# Cython>=3.0.0
# pyinstaller>=6.0.0
